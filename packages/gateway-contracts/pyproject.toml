[project]
name = "governed-llm-gateway-contracts"
version = "0.1.0"
description = "Provider-neutral immutable contracts for Governed LLM Gateway"
requires-python = ">=3.13,<3.15"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/governed_llm_gateway_contracts"]
