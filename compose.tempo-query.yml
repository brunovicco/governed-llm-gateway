services:
  tempo:
    volumes:
      - ./tests/integration/tempo-query.yaml:/etc/tempo/tempo.yaml:ro
    ports:
      - 127.0.0.1:3200:3200
    networks:
      - observability
      - tempo-query-host
  otel-collector:
    networks:
      - observability
      - tempo-query-host

networks:
  tempo-query-host: {}
