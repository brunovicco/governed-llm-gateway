receivers:
  otlp:
    protocols:
      http:
        endpoint: 0.0.0.0:4318

exporters:
  file/receipt:
    path: /receipts/traces.jsonl
    flush_interval: 100ms

service:
  pipelines:
    traces:
      receivers:
        - otlp
      exporters:
        - file/receipt
