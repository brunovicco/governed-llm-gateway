{
  "schema_version": "1.0",
  "benchmark_version": "multi-step-tool-use-v1",
  "data_classification": "public",
  "cases": [
    {
      "case_id": "multi-step-tool-use.account-status-notification.explicit.001",
      "workload": "multi_step_tool_use",
      "scorer": "multi_step_tool_use_v1",
      "prompt": "Plan the complete reviewed synthetic tool trajectory. Intermediate tool results are already supplied as immutable synthetic context; do not execute any tool. Return only JSON with the key steps, where every step contains exactly tool and arguments. Catalog: account_lookup, knowledge_search, send_notification, weather_forecast. Scenario: Use exactly these tool contracts and no extra arguments: account_lookup(account_id:string, include_history:boolean); knowledge_search(query:string, limit:integer); send_notification(user_id:string, message:string, channels:array[string]); weather_forecast(city:string, days:integer, units:string). Include every required call in order even when its immutable synthetic result is supplied; supplied results exist only to determine downstream arguments and never mean a call may be omitted. Read synthetic account A-104 without transaction history, then notify its owner by email only using the exact message 'Account A-104 is locked'. Immutable synthetic result context: account_lookup for A-104 returns owner U-7 and status locked.",
      "expected": {
        "steps": [
          {"tool": "account_lookup", "arguments": {"account_id": "A-104", "include_history": false}},
          {"tool": "send_notification", "arguments": {"user_id": "U-7", "message": "Account A-104 is locked", "channels": ["email"]}}
        ]
      },
      "metadata": {"contract_version": "1.0", "catalog_version": "support-tools-v1", "response_format": "multi_step_tool_calls_v1", "synthetic": true, "execute_tools": false}
    },
    {
      "case_id": "multi-step-tool-use.weather-notification.explicit.001",
      "workload": "multi_step_tool_use",
      "scorer": "multi_step_tool_use_v1",
      "prompt": "Plan the complete reviewed synthetic tool trajectory. Intermediate tool results are already supplied as immutable synthetic context; do not execute any tool. Return only JSON with the key steps, where every step contains exactly tool and arguments. Catalog: account_lookup, knowledge_search, send_notification, weather_forecast. Scenario: Use exactly these tool contracts and no extra arguments: account_lookup(account_id:string, include_history:boolean); knowledge_search(query:string, limit:integer); send_notification(user_id:string, message:string, channels:array[string]); weather_forecast(city:string, days:integer, units:string). Include every required call in order even when its immutable synthetic result is supplied; supplied results exist only to determine downstream arguments and never mean a call may be omitted. Read a one-day metric forecast for Example City, then notify synthetic user U-9 using exactly the channel token 'in_app' and the exact message 'Rain is expected tomorrow in Example City' when the reviewed forecast indicates rain. Immutable synthetic result context: weather_forecast for Example City returns rain expected tomorrow.",
      "expected": {
        "steps": [
          {"tool": "weather_forecast", "arguments": {"city": "Example City", "days": 1, "units": "metric"}},
          {"tool": "send_notification", "arguments": {"user_id": "U-9", "message": "Rain is expected tomorrow in Example City", "channels": ["in_app"]}}
        ]
      },
      "metadata": {"contract_version": "1.0", "catalog_version": "support-tools-v1", "response_format": "multi_step_tool_calls_v1", "synthetic": true, "execute_tools": false}
    },
    {
      "case_id": "multi-step-tool-use.knowledge-notification.explicit.001",
      "workload": "multi_step_tool_use",
      "scorer": "multi_step_tool_use_v1",
      "prompt": "Plan the complete reviewed synthetic tool trajectory. Intermediate tool results are already supplied as immutable synthetic context; do not execute any tool. Return only JSON with the key steps, where every step contains exactly tool and arguments. Catalog: account_lookup, knowledge_search, send_notification, weather_forecast. Scenario: Use exactly these tool contracts and no extra arguments: account_lookup(account_id:string, include_history:boolean); knowledge_search(query:string, limit:integer); send_notification(user_id:string, message:string, channels:array[string]); weather_forecast(city:string, days:integer, units:string). Include every required call in order even when its immutable synthetic result is supplied; supplied results exist only to determine downstream arguments and never mean a call may be omitted. Search for 'synthetic export feature' with at most three results, then notify synthetic user U-5 using channels email then in_app, in that order, with the exact message 'The export feature is available on the Reports page'. Immutable synthetic result context: knowledge_search returns that the export feature is available on the Reports page.",
      "expected": {
        "steps": [
          {"tool": "knowledge_search", "arguments": {"query": "synthetic export feature", "limit": 3}},
          {"tool": "send_notification", "arguments": {"user_id": "U-5", "message": "The export feature is available on the Reports page", "channels": ["email", "in_app"]}}
        ]
      },
      "metadata": {"contract_version": "1.0", "catalog_version": "support-tools-v1", "response_format": "multi_step_tool_calls_v1", "synthetic": true, "execute_tools": false}
    },
    {
      "case_id": "multi-step-tool-use.account-guide-notification.explicit.001",
      "workload": "multi_step_tool_use",
      "scorer": "multi_step_tool_use_v1",
      "prompt": "Plan the complete reviewed synthetic tool trajectory. Intermediate tool results are already supplied as immutable synthetic context; do not execute any tool. Return only JSON with the key steps, where every step contains exactly tool and arguments. Catalog: account_lookup, knowledge_search, send_notification, weather_forecast. Scenario: Use exactly these tool contracts and no extra arguments: account_lookup(account_id:string, include_history:boolean); knowledge_search(query:string, limit:integer); send_notification(user_id:string, message:string, channels:array[string]); weather_forecast(city:string, days:integer, units:string). Include every required call in order even when its immutable synthetic result is supplied; supplied results exist only to determine downstream arguments and never mean a call may be omitted. Read synthetic account A-204 without transaction history, search for 'synthetic premium recovery guide' with at most two results, then notify owner U-12 by email only using the exact message 'Use the premium recovery guide for account A-204'. Immutable synthetic result context: account_lookup returns owner U-12 and tier premium; knowledge_search for synthetic premium recovery guide returns the reviewed recovery guide.",
      "expected": {
        "steps": [
          {"tool": "account_lookup", "arguments": {"account_id": "A-204", "include_history": false}},
          {"tool": "knowledge_search", "arguments": {"query": "synthetic premium recovery guide", "limit": 2}},
          {"tool": "send_notification", "arguments": {"user_id": "U-12", "message": "Use the premium recovery guide for account A-204", "channels": ["email"]}}
        ]
      },
      "metadata": {"contract_version": "1.0", "catalog_version": "support-tools-v1", "response_format": "multi_step_tool_calls_v1", "synthetic": true, "execute_tools": false}
    }
  ]
}
